{
  "name": "dictale",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:next": "npm run dev --workspace logic-next",
    "dev:client": "npm run dev --workspace client-react",
    "dev:python": "cd packages/logic-python && uvicorn main:app --reload --port 8000",
    "build:next": "npm run build --workspace logic-next",
    "build:client": "npm run build --workspace client-react"
  }
}
